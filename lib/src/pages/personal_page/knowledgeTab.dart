import 'package:flutter/material.dart';

class KnowledgeTab extends StatelessWidget {
  const KnowledgeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Knowledge Management'),
    );
  }
}